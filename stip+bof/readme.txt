[data_extract_stip.py] extract useful information from JHU-ISI dataset.
All the variables could be adjust in [Global_Var.py]

we firstly execute [clusters_training.py], and notice that we may need to revise the N_CLUSTERS
which is 400 now.

then, we can execute the [classification].



